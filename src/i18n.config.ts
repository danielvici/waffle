// Picked up automatically by @nuxtjs/i18n as the vue-i18n runtime config
// (restructureDir is 'src', so this file belongs next to locales/).
//
// Without it vueI18nConfigs is empty and fallbackLocale stays unset, which
// means a key missing from the active locale renders as its raw path
// ("commandPalette.placeholder") instead of degrading to English.
export default defineI18nConfig(() => ({
  legacy: false,
  fallbackLocale: 'en',
}))
